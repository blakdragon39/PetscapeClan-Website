const notSeenToday = {
    label: 'Not seen today',
    filter: (c) => {

    }
}

const needsRankUp = {
    label: 'Needs rank up',
    filter: (c) => {

    }
}

const filters = {
    notSeenToday,
    needsRankUp
}

export default filters
