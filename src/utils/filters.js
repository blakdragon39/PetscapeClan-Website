const notSeenToday = {
    label: 'Not seen today',
    filter: (c) => c //todo
}

const needsRankUp = {
    label: 'Needs rank up',
    filter: (c) => c //todo
}


const filters = {
    notSeenToday,
    needsRankUp
}

export default filters
