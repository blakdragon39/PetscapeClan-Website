import React from 'react'
import PropTypes from 'prop-types'

const AddEditClanMemberPage = ({ clanMember }) => {
    return (
        <div>

        </div>
    )
}

AddEditClanMemberPage.propTypes = {
    clanMember: PropTypes.object
}

export default AddEditClanMemberPage
